# qa-repo

Runtime asset storage for the metaphactory `qa-instance2` environment.

This repository holds runtime configuration and assets (pages, templates,
queries, etc.) synced from metaphactory's git storage backend. Contents are
managed by the platform — avoid editing files here directly.
